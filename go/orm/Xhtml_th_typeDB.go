// generated by stacks/gong/go/models/orm_file_per_struct_back_repo.go
package orm

import (
	"database/sql"
	"encoding/json"
	"errors"
	"fmt"
	"io/ioutil"
	"log"
	"os"
	"path/filepath"
	"sort"
	"time"

	"gorm.io/gorm"

	"github.com/tealeg/xlsx/v3"

	"github.com/fullstack-lang/gongreqif/go/models"
)

// dummy variable to have the import declaration wihthout compile failure (even if no code needing this import is generated)
var dummy_Xhtml_th_type_sql sql.NullBool
var dummy_Xhtml_th_type_time time.Duration
var dummy_Xhtml_th_type_sort sort.Float64Slice

// Xhtml_th_typeAPI is the input in POST API
//
// for POST, API, one needs the fields of the model as well as the fields
// from associations ("Has One" and "Has Many") that are generated to
// fullfill the ORM requirements for associations
//
// swagger:model xhtml_th_typeAPI
type Xhtml_th_typeAPI struct {
	gorm.Model

	models.Xhtml_th_type_WOP

	// encoding of pointers
	// for API, it cannot be embedded
	Xhtml_th_typePointersEncoding Xhtml_th_typePointersEncoding
}

// Xhtml_th_typePointersEncoding encodes pointers to Struct and
// reverse pointers of slice of poitners to Struct
type Xhtml_th_typePointersEncoding struct {
	// insertion for pointer fields encoding declaration
}

// Xhtml_th_typeDB describes a xhtml_th_type in the database
//
// It incorporates the GORM ID, basic fields from the model (because they can be serialized),
// the encoded version of pointers
//
// swagger:model xhtml_th_typeDB
type Xhtml_th_typeDB struct {
	gorm.Model

	// insertion for basic fields declaration

	// Declation for basic field xhtml_th_typeDB.Name
	Name_Data sql.NullString
	
	// encoding of pointers
	// for GORM serialization, it is necessary to embed to Pointer Encoding declaration
	Xhtml_th_typePointersEncoding
}

// Xhtml_th_typeDBs arrays xhtml_th_typeDBs
// swagger:response xhtml_th_typeDBsResponse
type Xhtml_th_typeDBs []Xhtml_th_typeDB

// Xhtml_th_typeDBResponse provides response
// swagger:response xhtml_th_typeDBResponse
type Xhtml_th_typeDBResponse struct {
	Xhtml_th_typeDB
}

// Xhtml_th_typeWOP is a Xhtml_th_type without pointers (WOP is an acronym for "Without Pointers")
// it holds the same basic fields but pointers are encoded into uint
type Xhtml_th_typeWOP struct {
	ID int `xlsx:"0"`

	// insertion for WOP basic fields

	Name string `xlsx:"1"`
	// insertion for WOP pointer fields
}

var Xhtml_th_type_Fields = []string{
	// insertion for WOP basic fields
	"ID",
	"Name",
}

type BackRepoXhtml_th_typeStruct struct {
	// stores Xhtml_th_typeDB according to their gorm ID
	Map_Xhtml_th_typeDBID_Xhtml_th_typeDB map[uint]*Xhtml_th_typeDB

	// stores Xhtml_th_typeDB ID according to Xhtml_th_type address
	Map_Xhtml_th_typePtr_Xhtml_th_typeDBID map[*models.Xhtml_th_type]uint

	// stores Xhtml_th_type according to their gorm ID
	Map_Xhtml_th_typeDBID_Xhtml_th_typePtr map[uint]*models.Xhtml_th_type

	db *gorm.DB

	stage *models.StageStruct
}

func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) GetStage() (stage *models.StageStruct) {
	stage = backRepoXhtml_th_type.stage
	return
}

func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) GetDB() *gorm.DB {
	return backRepoXhtml_th_type.db
}

// GetXhtml_th_typeDBFromXhtml_th_typePtr is a handy function to access the back repo instance from the stage instance
func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) GetXhtml_th_typeDBFromXhtml_th_typePtr(xhtml_th_type *models.Xhtml_th_type) (xhtml_th_typeDB *Xhtml_th_typeDB) {
	id := backRepoXhtml_th_type.Map_Xhtml_th_typePtr_Xhtml_th_typeDBID[xhtml_th_type]
	xhtml_th_typeDB = backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typeDB[id]
	return
}

// BackRepoXhtml_th_type.CommitPhaseOne commits all staged instances of Xhtml_th_type to the BackRepo
// Phase One is the creation of instance in the database if it is not yet done to get the unique ID for each staged instance
func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) CommitPhaseOne(stage *models.StageStruct) (Error error) {

	for xhtml_th_type := range stage.Xhtml_th_types {
		backRepoXhtml_th_type.CommitPhaseOneInstance(xhtml_th_type)
	}

	// parse all backRepo instance and checks wether some instance have been unstaged
	// in this case, remove them from the back repo
	for id, xhtml_th_type := range backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typePtr {
		if _, ok := stage.Xhtml_th_types[xhtml_th_type]; !ok {
			backRepoXhtml_th_type.CommitDeleteInstance(id)
		}
	}

	return
}

// BackRepoXhtml_th_type.CommitDeleteInstance commits deletion of Xhtml_th_type to the BackRepo
func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) CommitDeleteInstance(id uint) (Error error) {

	xhtml_th_type := backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typePtr[id]

	// xhtml_th_type is not staged anymore, remove xhtml_th_typeDB
	xhtml_th_typeDB := backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typeDB[id]
	query := backRepoXhtml_th_type.db.Unscoped().Delete(&xhtml_th_typeDB)
	if query.Error != nil {
		log.Fatal(query.Error)
	}

	// update stores
	delete(backRepoXhtml_th_type.Map_Xhtml_th_typePtr_Xhtml_th_typeDBID, xhtml_th_type)
	delete(backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typePtr, id)
	delete(backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typeDB, id)

	return
}

// BackRepoXhtml_th_type.CommitPhaseOneInstance commits xhtml_th_type staged instances of Xhtml_th_type to the BackRepo
// Phase One is the creation of instance in the database if it is not yet done to get the unique ID for each staged instance
func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) CommitPhaseOneInstance(xhtml_th_type *models.Xhtml_th_type) (Error error) {

	// check if the xhtml_th_type is not commited yet
	if _, ok := backRepoXhtml_th_type.Map_Xhtml_th_typePtr_Xhtml_th_typeDBID[xhtml_th_type]; ok {
		return
	}

	// initiate xhtml_th_type
	var xhtml_th_typeDB Xhtml_th_typeDB
	xhtml_th_typeDB.CopyBasicFieldsFromXhtml_th_type(xhtml_th_type)

	query := backRepoXhtml_th_type.db.Create(&xhtml_th_typeDB)
	if query.Error != nil {
		log.Fatal(query.Error)
	}

	// update stores
	backRepoXhtml_th_type.Map_Xhtml_th_typePtr_Xhtml_th_typeDBID[xhtml_th_type] = xhtml_th_typeDB.ID
	backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typePtr[xhtml_th_typeDB.ID] = xhtml_th_type
	backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typeDB[xhtml_th_typeDB.ID] = &xhtml_th_typeDB

	return
}

// BackRepoXhtml_th_type.CommitPhaseTwo commits all staged instances of Xhtml_th_type to the BackRepo
// Phase Two is the update of instance with the field in the database
func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) CommitPhaseTwo(backRepo *BackRepoStruct) (Error error) {

	for idx, xhtml_th_type := range backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typePtr {
		backRepoXhtml_th_type.CommitPhaseTwoInstance(backRepo, idx, xhtml_th_type)
	}

	return
}

// BackRepoXhtml_th_type.CommitPhaseTwoInstance commits {{structname }} of models.Xhtml_th_type to the BackRepo
// Phase Two is the update of instance with the field in the database
func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) CommitPhaseTwoInstance(backRepo *BackRepoStruct, idx uint, xhtml_th_type *models.Xhtml_th_type) (Error error) {

	// fetch matching xhtml_th_typeDB
	if xhtml_th_typeDB, ok := backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typeDB[idx]; ok {

		xhtml_th_typeDB.CopyBasicFieldsFromXhtml_th_type(xhtml_th_type)

		// insertion point for translating pointers encodings into actual pointers
		query := backRepoXhtml_th_type.db.Save(&xhtml_th_typeDB)
		if query.Error != nil {
			log.Fatalln(query.Error)
		}

	} else {
		err := errors.New(
			fmt.Sprintf("Unkown Xhtml_th_type intance %s", xhtml_th_type.Name))
		return err
	}

	return
}

// BackRepoXhtml_th_type.CheckoutPhaseOne Checkouts all BackRepo instances to the Stage
//
// Phase One will result in having instances on the stage aligned with the back repo
// pointers are not initialized yet (this is for phase two)
func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) CheckoutPhaseOne() (Error error) {

	xhtml_th_typeDBArray := make([]Xhtml_th_typeDB, 0)
	query := backRepoXhtml_th_type.db.Find(&xhtml_th_typeDBArray)
	if query.Error != nil {
		return query.Error
	}

	// list of instances to be removed
	// start from the initial map on the stage and remove instances that have been checked out
	xhtml_th_typeInstancesToBeRemovedFromTheStage := make(map[*models.Xhtml_th_type]any)
	for key, value := range backRepoXhtml_th_type.stage.Xhtml_th_types {
		xhtml_th_typeInstancesToBeRemovedFromTheStage[key] = value
	}

	// copy orm objects to the the map
	for _, xhtml_th_typeDB := range xhtml_th_typeDBArray {
		backRepoXhtml_th_type.CheckoutPhaseOneInstance(&xhtml_th_typeDB)

		// do not remove this instance from the stage, therefore
		// remove instance from the list of instances to be be removed from the stage
		xhtml_th_type, ok := backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typePtr[xhtml_th_typeDB.ID]
		if ok {
			delete(xhtml_th_typeInstancesToBeRemovedFromTheStage, xhtml_th_type)
		}
	}

	// remove from stage and back repo's 3 maps all xhtml_th_types that are not in the checkout
	for xhtml_th_type := range xhtml_th_typeInstancesToBeRemovedFromTheStage {
		xhtml_th_type.Unstage(backRepoXhtml_th_type.GetStage())

		// remove instance from the back repo 3 maps
		xhtml_th_typeID := backRepoXhtml_th_type.Map_Xhtml_th_typePtr_Xhtml_th_typeDBID[xhtml_th_type]
		delete(backRepoXhtml_th_type.Map_Xhtml_th_typePtr_Xhtml_th_typeDBID, xhtml_th_type)
		delete(backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typeDB, xhtml_th_typeID)
		delete(backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typePtr, xhtml_th_typeID)
	}

	return
}

// CheckoutPhaseOneInstance takes a xhtml_th_typeDB that has been found in the DB, updates the backRepo and stages the
// models version of the xhtml_th_typeDB
func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) CheckoutPhaseOneInstance(xhtml_th_typeDB *Xhtml_th_typeDB) (Error error) {

	xhtml_th_type, ok := backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typePtr[xhtml_th_typeDB.ID]
	if !ok {
		xhtml_th_type = new(models.Xhtml_th_type)

		backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typePtr[xhtml_th_typeDB.ID] = xhtml_th_type
		backRepoXhtml_th_type.Map_Xhtml_th_typePtr_Xhtml_th_typeDBID[xhtml_th_type] = xhtml_th_typeDB.ID

		// append model store with the new element
		xhtml_th_type.Name = xhtml_th_typeDB.Name_Data.String
		xhtml_th_type.Stage(backRepoXhtml_th_type.GetStage())
	}
	xhtml_th_typeDB.CopyBasicFieldsToXhtml_th_type(xhtml_th_type)

	// in some cases, the instance might have been unstaged. It is necessary to stage it again
	xhtml_th_type.Stage(backRepoXhtml_th_type.GetStage())

	// preserve pointer to xhtml_th_typeDB. Otherwise, pointer will is recycled and the map of pointers
	// Map_Xhtml_th_typeDBID_Xhtml_th_typeDB)[xhtml_th_typeDB hold variable pointers
	xhtml_th_typeDB_Data := *xhtml_th_typeDB
	preservedPtrToXhtml_th_type := &xhtml_th_typeDB_Data
	backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typeDB[xhtml_th_typeDB.ID] = preservedPtrToXhtml_th_type

	return
}

// BackRepoXhtml_th_type.CheckoutPhaseTwo Checkouts all staged instances of Xhtml_th_type to the BackRepo
// Phase Two is the update of instance with the field in the database
func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) CheckoutPhaseTwo(backRepo *BackRepoStruct) (Error error) {

	// parse all DB instance and update all pointer fields of the translated models instance
	for _, xhtml_th_typeDB := range backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typeDB {
		backRepoXhtml_th_type.CheckoutPhaseTwoInstance(backRepo, xhtml_th_typeDB)
	}
	return
}

// BackRepoXhtml_th_type.CheckoutPhaseTwoInstance Checkouts staged instances of Xhtml_th_type to the BackRepo
// Phase Two is the update of instance with the field in the database
func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) CheckoutPhaseTwoInstance(backRepo *BackRepoStruct, xhtml_th_typeDB *Xhtml_th_typeDB) (Error error) {

	xhtml_th_type := backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typePtr[xhtml_th_typeDB.ID]

	xhtml_th_typeDB.DecodePointers(backRepo, xhtml_th_type)

	return
}

func (xhtml_th_typeDB *Xhtml_th_typeDB) DecodePointers(backRepo *BackRepoStruct, xhtml_th_type *models.Xhtml_th_type) {

	// insertion point for checkout of pointer encoding
	return
}

// CommitXhtml_th_type allows commit of a single xhtml_th_type (if already staged)
func (backRepo *BackRepoStruct) CommitXhtml_th_type(xhtml_th_type *models.Xhtml_th_type) {
	backRepo.BackRepoXhtml_th_type.CommitPhaseOneInstance(xhtml_th_type)
	if id, ok := backRepo.BackRepoXhtml_th_type.Map_Xhtml_th_typePtr_Xhtml_th_typeDBID[xhtml_th_type]; ok {
		backRepo.BackRepoXhtml_th_type.CommitPhaseTwoInstance(backRepo, id, xhtml_th_type)
	}
	backRepo.CommitFromBackNb = backRepo.CommitFromBackNb + 1
}

// CommitXhtml_th_type allows checkout of a single xhtml_th_type (if already staged and with a BackRepo id)
func (backRepo *BackRepoStruct) CheckoutXhtml_th_type(xhtml_th_type *models.Xhtml_th_type) {
	// check if the xhtml_th_type is staged
	if _, ok := backRepo.BackRepoXhtml_th_type.Map_Xhtml_th_typePtr_Xhtml_th_typeDBID[xhtml_th_type]; ok {

		if id, ok := backRepo.BackRepoXhtml_th_type.Map_Xhtml_th_typePtr_Xhtml_th_typeDBID[xhtml_th_type]; ok {
			var xhtml_th_typeDB Xhtml_th_typeDB
			xhtml_th_typeDB.ID = id

			if err := backRepo.BackRepoXhtml_th_type.db.First(&xhtml_th_typeDB, id).Error; err != nil {
				log.Fatalln("CheckoutXhtml_th_type : Problem with getting object with id:", id)
			}
			backRepo.BackRepoXhtml_th_type.CheckoutPhaseOneInstance(&xhtml_th_typeDB)
			backRepo.BackRepoXhtml_th_type.CheckoutPhaseTwoInstance(backRepo, &xhtml_th_typeDB)
		}
	}
}

// CopyBasicFieldsFromXhtml_th_type
func (xhtml_th_typeDB *Xhtml_th_typeDB) CopyBasicFieldsFromXhtml_th_type(xhtml_th_type *models.Xhtml_th_type) {
	// insertion point for fields commit

	xhtml_th_typeDB.Name_Data.String = xhtml_th_type.Name
	xhtml_th_typeDB.Name_Data.Valid = true
}

// CopyBasicFieldsFromXhtml_th_type_WOP
func (xhtml_th_typeDB *Xhtml_th_typeDB) CopyBasicFieldsFromXhtml_th_type_WOP(xhtml_th_type *models.Xhtml_th_type_WOP) {
	// insertion point for fields commit

	xhtml_th_typeDB.Name_Data.String = xhtml_th_type.Name
	xhtml_th_typeDB.Name_Data.Valid = true
}

// CopyBasicFieldsFromXhtml_th_typeWOP
func (xhtml_th_typeDB *Xhtml_th_typeDB) CopyBasicFieldsFromXhtml_th_typeWOP(xhtml_th_type *Xhtml_th_typeWOP) {
	// insertion point for fields commit

	xhtml_th_typeDB.Name_Data.String = xhtml_th_type.Name
	xhtml_th_typeDB.Name_Data.Valid = true
}

// CopyBasicFieldsToXhtml_th_type
func (xhtml_th_typeDB *Xhtml_th_typeDB) CopyBasicFieldsToXhtml_th_type(xhtml_th_type *models.Xhtml_th_type) {
	// insertion point for checkout of basic fields (back repo to stage)
	xhtml_th_type.Name = xhtml_th_typeDB.Name_Data.String
}

// CopyBasicFieldsToXhtml_th_type_WOP
func (xhtml_th_typeDB *Xhtml_th_typeDB) CopyBasicFieldsToXhtml_th_type_WOP(xhtml_th_type *models.Xhtml_th_type_WOP) {
	// insertion point for checkout of basic fields (back repo to stage)
	xhtml_th_type.Name = xhtml_th_typeDB.Name_Data.String
}

// CopyBasicFieldsToXhtml_th_typeWOP
func (xhtml_th_typeDB *Xhtml_th_typeDB) CopyBasicFieldsToXhtml_th_typeWOP(xhtml_th_type *Xhtml_th_typeWOP) {
	xhtml_th_type.ID = int(xhtml_th_typeDB.ID)
	// insertion point for checkout of basic fields (back repo to stage)
	xhtml_th_type.Name = xhtml_th_typeDB.Name_Data.String
}

// Backup generates a json file from a slice of all Xhtml_th_typeDB instances in the backrepo
func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) Backup(dirPath string) {

	filename := filepath.Join(dirPath, "Xhtml_th_typeDB.json")

	// organize the map into an array with increasing IDs, in order to have repoductible
	// backup file
	forBackup := make([]*Xhtml_th_typeDB, 0)
	for _, xhtml_th_typeDB := range backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typeDB {
		forBackup = append(forBackup, xhtml_th_typeDB)
	}

	sort.Slice(forBackup[:], func(i, j int) bool {
		return forBackup[i].ID < forBackup[j].ID
	})

	file, err := json.MarshalIndent(forBackup, "", " ")

	if err != nil {
		log.Fatal("Cannot json Xhtml_th_type ", filename, " ", err.Error())
	}

	err = ioutil.WriteFile(filename, file, 0644)
	if err != nil {
		log.Fatal("Cannot write the json Xhtml_th_type file", err.Error())
	}
}

// Backup generates a json file from a slice of all Xhtml_th_typeDB instances in the backrepo
func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) BackupXL(file *xlsx.File) {

	// organize the map into an array with increasing IDs, in order to have repoductible
	// backup file
	forBackup := make([]*Xhtml_th_typeDB, 0)
	for _, xhtml_th_typeDB := range backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typeDB {
		forBackup = append(forBackup, xhtml_th_typeDB)
	}

	sort.Slice(forBackup[:], func(i, j int) bool {
		return forBackup[i].ID < forBackup[j].ID
	})

	sh, err := file.AddSheet("Xhtml_th_type")
	if err != nil {
		log.Fatal("Cannot add XL file", err.Error())
	}
	_ = sh

	row := sh.AddRow()
	row.WriteSlice(&Xhtml_th_type_Fields, -1)
	for _, xhtml_th_typeDB := range forBackup {

		var xhtml_th_typeWOP Xhtml_th_typeWOP
		xhtml_th_typeDB.CopyBasicFieldsToXhtml_th_typeWOP(&xhtml_th_typeWOP)

		row := sh.AddRow()
		row.WriteStruct(&xhtml_th_typeWOP, -1)
	}
}

// RestoreXL from the "Xhtml_th_type" sheet all Xhtml_th_typeDB instances
func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) RestoreXLPhaseOne(file *xlsx.File) {

	// resets the map
	BackRepoXhtml_th_typeid_atBckpTime_newID = make(map[uint]uint)

	sh, ok := file.Sheet["Xhtml_th_type"]
	_ = sh
	if !ok {
		log.Fatal(errors.New("sheet not found"))
	}

	// log.Println("Max row is", sh.MaxRow)
	err := sh.ForEachRow(backRepoXhtml_th_type.rowVisitorXhtml_th_type)
	if err != nil {
		log.Fatal("Err=", err)
	}
}

func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) rowVisitorXhtml_th_type(row *xlsx.Row) error {

	log.Printf("row line %d\n", row.GetCoordinate())
	log.Println(row)

	// skip first line
	if row.GetCoordinate() > 0 {
		var xhtml_th_typeWOP Xhtml_th_typeWOP
		row.ReadStruct(&xhtml_th_typeWOP)

		// add the unmarshalled struct to the stage
		xhtml_th_typeDB := new(Xhtml_th_typeDB)
		xhtml_th_typeDB.CopyBasicFieldsFromXhtml_th_typeWOP(&xhtml_th_typeWOP)

		xhtml_th_typeDB_ID_atBackupTime := xhtml_th_typeDB.ID
		xhtml_th_typeDB.ID = 0
		query := backRepoXhtml_th_type.db.Create(xhtml_th_typeDB)
		if query.Error != nil {
			log.Fatal(query.Error)
		}
		backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typeDB[xhtml_th_typeDB.ID] = xhtml_th_typeDB
		BackRepoXhtml_th_typeid_atBckpTime_newID[xhtml_th_typeDB_ID_atBackupTime] = xhtml_th_typeDB.ID
	}
	return nil
}

// RestorePhaseOne read the file "Xhtml_th_typeDB.json" in dirPath that stores an array
// of Xhtml_th_typeDB and stores it in the database
// the map BackRepoXhtml_th_typeid_atBckpTime_newID is updated accordingly
func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) RestorePhaseOne(dirPath string) {

	// resets the map
	BackRepoXhtml_th_typeid_atBckpTime_newID = make(map[uint]uint)

	filename := filepath.Join(dirPath, "Xhtml_th_typeDB.json")
	jsonFile, err := os.Open(filename)
	// if we os.Open returns an error then handle it
	if err != nil {
		log.Fatal("Cannot restore/open the json Xhtml_th_type file", filename, " ", err.Error())
	}

	// read our opened jsonFile as a byte array.
	byteValue, _ := ioutil.ReadAll(jsonFile)

	var forRestore []*Xhtml_th_typeDB

	err = json.Unmarshal(byteValue, &forRestore)

	// fill up Map_Xhtml_th_typeDBID_Xhtml_th_typeDB
	for _, xhtml_th_typeDB := range forRestore {

		xhtml_th_typeDB_ID_atBackupTime := xhtml_th_typeDB.ID
		xhtml_th_typeDB.ID = 0
		query := backRepoXhtml_th_type.db.Create(xhtml_th_typeDB)
		if query.Error != nil {
			log.Fatal(query.Error)
		}
		backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typeDB[xhtml_th_typeDB.ID] = xhtml_th_typeDB
		BackRepoXhtml_th_typeid_atBckpTime_newID[xhtml_th_typeDB_ID_atBackupTime] = xhtml_th_typeDB.ID
	}

	if err != nil {
		log.Fatal("Cannot restore/unmarshall json Xhtml_th_type file", err.Error())
	}
}

// RestorePhaseTwo uses all map BackRepo<Xhtml_th_type>id_atBckpTime_newID
// to compute new index
func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) RestorePhaseTwo() {

	for _, xhtml_th_typeDB := range backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typeDB {

		// next line of code is to avert unused variable compilation error
		_ = xhtml_th_typeDB

		// insertion point for reindexing pointers encoding
		// update databse with new index encoding
		query := backRepoXhtml_th_type.db.Model(xhtml_th_typeDB).Updates(*xhtml_th_typeDB)
		if query.Error != nil {
			log.Fatal(query.Error)
		}
	}

}

// BackRepoXhtml_th_type.ResetReversePointers commits all staged instances of Xhtml_th_type to the BackRepo
// Phase Two is the update of instance with the field in the database
func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) ResetReversePointers(backRepo *BackRepoStruct) (Error error) {

	for idx, xhtml_th_type := range backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typePtr {
		backRepoXhtml_th_type.ResetReversePointersInstance(backRepo, idx, xhtml_th_type)
	}

	return
}

func (backRepoXhtml_th_type *BackRepoXhtml_th_typeStruct) ResetReversePointersInstance(backRepo *BackRepoStruct, idx uint, xhtml_th_type *models.Xhtml_th_type) (Error error) {

	// fetch matching xhtml_th_typeDB
	if xhtml_th_typeDB, ok := backRepoXhtml_th_type.Map_Xhtml_th_typeDBID_Xhtml_th_typeDB[idx]; ok {
		_ = xhtml_th_typeDB // to avoid unused variable error if there are no reverse to reset

		// insertion point for reverse pointers reset
		// end of insertion point for reverse pointers reset
	}

	return
}

// this field is used during the restauration process.
// it stores the ID at the backup time and is used for renumbering
var BackRepoXhtml_th_typeid_atBckpTime_newID map[uint]uint
