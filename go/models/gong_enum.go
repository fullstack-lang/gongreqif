// generated code - do not edit
package models

// insertion point of enum utility functions
// Utility function for AnyURI
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (anyuri AnyURI) ToString() (res string) {

	// migration of former implementation of enum
	switch anyuri {
	// insertion code per enum code
	}
	return
}

func (anyuri *AnyURI) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (anyuri *AnyURI) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (anyuri *AnyURI) ToCodeString() (res string) {

	switch *anyuri {
	// insertion code per enum code
	}
	return
}

func (anyuri AnyURI) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (anyuri AnyURI) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for CDATA
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (cdata CDATA) ToString() (res string) {

	// migration of former implementation of enum
	switch cdata {
	// insertion code per enum code
	}
	return
}

func (cdata *CDATA) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (cdata *CDATA) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (cdata *CDATA) ToCodeString() (res string) {

	switch *cdata {
	// insertion code per enum code
	}
	return
}

func (cdata CDATA) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (cdata CDATA) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for CURIE
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (curie CURIE) ToString() (res string) {

	// migration of former implementation of enum
	switch curie {
	// insertion code per enum code
	}
	return
}

func (curie *CURIE) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (curie *CURIE) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (curie *CURIE) ToCodeString() (res string) {

	switch *curie {
	// insertion code per enum code
	}
	return
}

func (curie CURIE) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (curie CURIE) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for Character
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (character Character) ToString() (res string) {

	// migration of former implementation of enum
	switch character {
	// insertion code per enum code
	}
	return
}

func (character *Character) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (character *Character) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (character *Character) ToCodeString() (res string) {

	switch *character {
	// insertion code per enum code
	}
	return
}

func (character Character) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (character Character) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for Charset
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (charset Charset) ToString() (res string) {

	// migration of former implementation of enum
	switch charset {
	// insertion code per enum code
	}
	return
}

func (charset *Charset) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (charset *Charset) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (charset *Charset) ToCodeString() (res string) {

	switch *charset {
	// insertion code per enum code
	}
	return
}

func (charset Charset) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (charset Charset) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for Color
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (color Color) ToString() (res string) {

	// migration of former implementation of enum
	switch color {
	// insertion code per enum code
	}
	return
}

func (color *Color) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (color *Color) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (color *Color) ToCodeString() (res string) {

	switch *color {
	// insertion code per enum code
	}
	return
}

func (color Color) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (color Color) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for ContentType
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (contenttype ContentType) ToString() (res string) {

	// migration of former implementation of enum
	switch contenttype {
	// insertion code per enum code
	}
	return
}

func (contenttype *ContentType) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (contenttype *ContentType) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (contenttype *ContentType) ToCodeString() (res string) {

	switch *contenttype {
	// insertion code per enum code
	}
	return
}

func (contenttype ContentType) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (contenttype ContentType) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for ContentTypes
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (contenttypes ContentTypes) ToString() (res string) {

	// migration of former implementation of enum
	switch contenttypes {
	// insertion code per enum code
	}
	return
}

func (contenttypes *ContentTypes) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (contenttypes *ContentTypes) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (contenttypes *ContentTypes) ToCodeString() (res string) {

	switch *contenttypes {
	// insertion code per enum code
	}
	return
}

func (contenttypes ContentTypes) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (contenttypes ContentTypes) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for EmptyString
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (emptystring EmptyString) ToString() (res string) {

	// migration of former implementation of enum
	switch emptystring {
	// insertion code per enum code
	}
	return
}

func (emptystring *EmptyString) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (emptystring *EmptyString) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (emptystring *EmptyString) ToCodeString() (res string) {

	switch *emptystring {
	// insertion code per enum code
	}
	return
}

func (emptystring EmptyString) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (emptystring EmptyString) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for FrameTarget
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (frametarget FrameTarget) ToString() (res string) {

	// migration of former implementation of enum
	switch frametarget {
	// insertion code per enum code
	}
	return
}

func (frametarget *FrameTarget) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (frametarget *FrameTarget) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (frametarget *FrameTarget) ToCodeString() (res string) {

	switch *frametarget {
	// insertion code per enum code
	}
	return
}

func (frametarget FrameTarget) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (frametarget FrameTarget) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for GLOBAL_REF
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (global_ref GLOBAL_REF) ToString() (res string) {

	// migration of former implementation of enum
	switch global_ref {
	// insertion code per enum code
	}
	return
}

func (global_ref *GLOBAL_REF) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (global_ref *GLOBAL_REF) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (global_ref *GLOBAL_REF) ToCodeString() (res string) {

	switch *global_ref {
	// insertion code per enum code
	}
	return
}

func (global_ref GLOBAL_REF) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (global_ref GLOBAL_REF) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for ID
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (id ID) ToString() (res string) {

	// migration of former implementation of enum
	switch id {
	// insertion code per enum code
	}
	return
}

func (id *ID) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (id *ID) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (id *ID) ToCodeString() (res string) {

	switch *id {
	// insertion code per enum code
	}
	return
}

func (id ID) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (id ID) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for IDREF
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (idref IDREF) ToString() (res string) {

	// migration of former implementation of enum
	switch idref {
	// insertion code per enum code
	}
	return
}

func (idref *IDREF) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (idref *IDREF) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (idref *IDREF) ToCodeString() (res string) {

	switch *idref {
	// insertion code per enum code
	}
	return
}

func (idref IDREF) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (idref IDREF) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for Language
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (language Language) ToString() (res string) {

	// migration of former implementation of enum
	switch language {
	// insertion code per enum code
	}
	return
}

func (language *Language) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (language *Language) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (language *Language) ToCodeString() (res string) {

	switch *language {
	// insertion code per enum code
	}
	return
}

func (language Language) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (language Language) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for LanguageCodes
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (languagecodes LanguageCodes) ToString() (res string) {

	// migration of former implementation of enum
	switch languagecodes {
	// insertion code per enum code
	}
	return
}

func (languagecodes *LanguageCodes) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (languagecodes *LanguageCodes) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (languagecodes *LanguageCodes) ToCodeString() (res string) {

	switch *languagecodes {
	// insertion code per enum code
	}
	return
}

func (languagecodes LanguageCodes) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (languagecodes LanguageCodes) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for Length
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (length Length) ToString() (res string) {

	// migration of former implementation of enum
	switch length {
	// insertion code per enum code
	}
	return
}

func (length *Length) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (length *Length) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (length *Length) ToCodeString() (res string) {

	switch *length {
	// insertion code per enum code
	}
	return
}

func (length Length) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (length Length) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for MediaDesc
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (mediadesc MediaDesc) ToString() (res string) {

	// migration of former implementation of enum
	switch mediadesc {
	// insertion code per enum code
	}
	return
}

func (mediadesc *MediaDesc) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (mediadesc *MediaDesc) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (mediadesc *MediaDesc) ToCodeString() (res string) {

	switch *mediadesc {
	// insertion code per enum code
	}
	return
}

func (mediadesc MediaDesc) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (mediadesc MediaDesc) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for MultiLength
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (multilength MultiLength) ToString() (res string) {

	// migration of former implementation of enum
	switch multilength {
	// insertion code per enum code
	}
	return
}

func (multilength *MultiLength) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (multilength *MultiLength) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (multilength *MultiLength) ToCodeString() (res string) {

	switch *multilength {
	// insertion code per enum code
	}
	return
}

func (multilength MultiLength) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (multilength MultiLength) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for MultiLengths
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (multilengths MultiLengths) ToString() (res string) {

	// migration of former implementation of enum
	switch multilengths {
	// insertion code per enum code
	}
	return
}

func (multilengths *MultiLengths) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (multilengths *MultiLengths) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (multilengths *MultiLengths) ToCodeString() (res string) {

	switch *multilengths {
	// insertion code per enum code
	}
	return
}

func (multilengths MultiLengths) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (multilengths MultiLengths) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for NCName
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (ncname NCName) ToString() (res string) {

	// migration of former implementation of enum
	switch ncname {
	// insertion code per enum code
	}
	return
}

func (ncname *NCName) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (ncname *NCName) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (ncname *NCName) ToCodeString() (res string) {

	switch *ncname {
	// insertion code per enum code
	}
	return
}

func (ncname NCName) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (ncname NCName) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for NMTOKEN
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (nmtoken NMTOKEN) ToInt() (res int) {

	// migration of former implementation of enum
	switch nmtoken {
	// insertion code per enum code
	}
	return
}

func (nmtoken *NMTOKEN) FromInt(input int) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (nmtoken *NMTOKEN) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (nmtoken *NMTOKEN) ToCodeString() (res string) {

	switch *nmtoken {
	// insertion code per enum code
	}
	return
}

func (nmtoken NMTOKEN) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (nmtoken NMTOKEN) CodeValues() (res []int) {

	res = make([]int, 0)

	// insertion code per enum code

	return
}

// Utility function for NormalizedString
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (normalizedstring NormalizedString) ToString() (res string) {

	// migration of former implementation of enum
	switch normalizedstring {
	// insertion code per enum code
	}
	return
}

func (normalizedstring *NormalizedString) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (normalizedstring *NormalizedString) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (normalizedstring *NormalizedString) ToCodeString() (res string) {

	switch *normalizedstring {
	// insertion code per enum code
	}
	return
}

func (normalizedstring NormalizedString) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (normalizedstring NormalizedString) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for SafeCURIE
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (safecurie SafeCURIE) ToString() (res string) {

	// migration of former implementation of enum
	switch safecurie {
	// insertion code per enum code
	}
	return
}

func (safecurie *SafeCURIE) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (safecurie *SafeCURIE) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (safecurie *SafeCURIE) ToCodeString() (res string) {

	switch *safecurie {
	// insertion code per enum code
	}
	return
}

func (safecurie SafeCURIE) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (safecurie SafeCURIE) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for Script
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (script Script) ToString() (res string) {

	// migration of former implementation of enum
	switch script {
	// insertion code per enum code
	}
	return
}

func (script *Script) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (script *Script) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (script *Script) ToCodeString() (res string) {

	switch *script {
	// insertion code per enum code
	}
	return
}

func (script Script) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (script Script) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for Text
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (text Text) ToString() (res string) {

	// migration of former implementation of enum
	switch text {
	// insertion code per enum code
	}
	return
}

func (text *Text) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (text *Text) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (text *Text) ToCodeString() (res string) {

	switch *text {
	// insertion code per enum code
	}
	return
}

func (text Text) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (text Text) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for URIREF
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (uriref URIREF) ToString() (res string) {

	// migration of former implementation of enum
	switch uriref {
	// insertion code per enum code
	}
	return
}

func (uriref *URIREF) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (uriref *URIREF) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (uriref *URIREF) ToCodeString() (res string) {

	switch *uriref {
	// insertion code per enum code
	}
	return
}

func (uriref URIREF) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (uriref URIREF) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// Utility function for URIorSafeCURIE
// if enum values are string, it is stored with the value
// if enum values are int, they are stored with the code of the value
func (uriorsafecurie URIorSafeCURIE) ToString() (res string) {

	// migration of former implementation of enum
	switch uriorsafecurie {
	// insertion code per enum code
	}
	return
}

func (uriorsafecurie *URIorSafeCURIE) FromString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (uriorsafecurie *URIorSafeCURIE) FromCodeString(input string) (err error) {

	switch input {
	// insertion code per enum code
	default:
		return errUnkownEnum
	}
	return
}

func (uriorsafecurie *URIorSafeCURIE) ToCodeString() (res string) {

	switch *uriorsafecurie {
	// insertion code per enum code
	}
	return
}

func (uriorsafecurie URIorSafeCURIE) Codes() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

func (uriorsafecurie URIorSafeCURIE) CodeValues() (res []string) {

	res = make([]string, 0)

	// insertion code per enum code

	return
}

// end of insertion point for enum utility functions

type GongstructEnumStringField interface {
	string | AnyURI | CDATA | CURIE | Character | Charset | Color | ContentType | ContentTypes | EmptyString | FrameTarget | GLOBAL_REF | ID | IDREF | Language | LanguageCodes | Length | MediaDesc | MultiLength | MultiLengths | NCName | NormalizedString | SafeCURIE | Script | Text | URIREF | URIorSafeCURIE
	Codes() []string
	CodeValues() []string
}

type PointerToGongstructEnumStringField interface {
	*AnyURI | *CDATA | *CURIE | *Character | *Charset | *Color | *ContentType | *ContentTypes | *EmptyString | *FrameTarget | *GLOBAL_REF | *ID | *IDREF | *Language | *LanguageCodes | *Length | *MediaDesc | *MultiLength | *MultiLengths | *NCName | *NormalizedString | *SafeCURIE | *Script | *Text | *URIREF | *URIorSafeCURIE
	FromCodeString(input string) (err error)
}

type GongstructEnumIntField interface {
	int | NMTOKEN
	Codes() []string
	CodeValues() []int
}

type PointerToGongstructEnumIntField interface {
	*NMTOKEN
	FromCodeString(input string) (err error)
}

// Last line of the template
